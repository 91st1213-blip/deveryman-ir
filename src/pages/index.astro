---
import { getLatestEarnings } from '../lib/sheets';

// Google Sheets からデータを取得
const earningsData = await getLatestEarnings();

// 企業IDからカラーを取得
const COMPANY_COLORS: Record<string, string> = {
  mitsui: '#0066CC',
  mitsubishi: '#DC143C',
  sumitomo: '#00A550',
  tokyo: '#7B68EE',
  nomura: '#FF8C00',
  tokyu: '#FF1493',
  hulic: '#FFD700',
};

// カード表示用データを生成
const companies = earningsData.map(item => ({
  id: item.companyId,
  name: item.company,
  fiscalMonth: item.period.includes('/3') ? 3 : 12,
  color: COMPANY_COLORS[item.companyId] || '#666666',
  revenue: item.revenue,
  profit: item.operatingProfit,
}));
---
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>デベロッパー決算まとめ | デベリーマン4.0</title>
  <meta name="description" content="大手不動産デベロッパー7社の最新決算データを一覧表示">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Hiragino Sans', sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 2rem 1rem;
    }
    .container { max-width: 1400px; margin: 0 auto; }
    h1 {
      color: white;
      font-size: 2.5rem;
      text-align: center;
      margin-bottom: 3rem;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }
    .companies-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
      margin-bottom: 3rem;
    }
    .company-card {
      background: white;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 10px 30px rgba(0,0,0,0.2);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .company-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 15px 40px rgba(0,0,0,0.3);
    }
    .card-header {
      padding: 1.5rem;
      color: white;
      text-align: center;
    }
    .company-name {
      font-size: 1.5rem;
      font-weight: bold;
      margin-bottom: 0.5rem;
    }
    .fiscal-badge {
      display: inline-block;
      background: rgba(255,255,255,0.2);
      backdrop-filter: blur(10px);
      padding: 0.5rem 1rem;
      border-radius: 20px;
      font-size: 0.9rem;
    }
    .card-body { padding: 1.5rem; }
    .metrics {
      display: flex;
      justify-content: space-around;
      gap: 1rem;
    }
    .metric { text-align: center; }
    .metric-label {
      font-size: 0.85rem;
      color: #666;
      margin-bottom: 0.5rem;
    }
    .metric-value {
      font-size: 1.8rem;
      font-weight: bold;
      color: #333;
    }
    .metric-unit {
      font-size: 0.9rem;
      color: #999;
      margin-left: 0.2rem;
    }
    .table-container {
      background: white;
      border-radius: 16px;
      padding: 2rem;
      box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    }
    h2 {
      font-size: 1.8rem;
      color: #333;
      margin-bottom: 1.5rem;
    }
    table {
      width: 100%;
      border-collapse: collapse;
    }
    th {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 1rem;
      text-align: left;
      font-weight: 600;
    }
    td {
      padding: 1rem;
      border-bottom: 1px solid #eee;
    }
    tr:hover { background: #f8f9fa; }
    .number { text-align: right; }
    @media (max-width: 768px) {
      h1 { font-size: 1.8rem; }
      .companies-grid { grid-template-columns: 1fr; }
      .metric-value { font-size: 1.5rem; }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🏢 デベロッパー決算まとめ</h1>
    
    <div class="companies-grid">
      {companies.map(company => (
        <div class="company-card">
          <div class="card-header" style={`background: ${company.color};`}>
            <div class="company-name">{company.name}</div>
            <div class="fiscal-info">
              <span class="fiscal-badge">📅 {company.fiscalMonth}月決算</span>
            </div>
          </div>
          <div class="card-body">
            <div class="metrics">
              <div class="metric">
                <div class="metric-label">営業収益</div>
                <div class="metric-value">
                  {(company.revenue / 10000).toFixed(1)}
                  <span class="metric-unit">兆円</span>
                </div>
              </div>
              <div class="metric">
                <div class="metric-label">営業利益</div>
                <div class="metric-value">
                  {company.profit.toLocaleString()}
                  <span class="metric-unit">億円</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>

    <div class="table-container">
      <h2>📊 最新決算データ</h2>
      <table>
        <thead>
          <tr>
            <th>企業名</th>
            <th>決算期</th>
            <th class="number">営業収益（億円）</th>
            <th class="number">営業利益（億円）</th>
            <th class="number">経常利益（億円）</th>
            <th class="number">純利益（億円）</th>
            <th class="number">利益率（%）</th>
          </tr>
        </thead>
        <tbody>
          {earningsData.map(item => (
            <tr>
              <td><strong>{item.company}</strong></td>
              <td>{item.period}</td>
              <td class="number">{item.revenue.toLocaleString()}</td>
              <td class="number">{item.operatingProfit.toLocaleString()}</td>
              <td class="number">{item.ordinaryProfit.toLocaleString()}</td>
              <td class="number">{item.netProfit.toLocaleString()}</td>
              <td class="number">{item.profitMargin.toFixed(1)}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  </div>
</body>
</html>
